<!DOCTYPE html>
<html>
  <head>
    <title>SurvivalGuide</title>
     <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
  </head>

  <body class="code">

   <div id="custom-bootstrap-menu" class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">

        <div class="navbar-header">
          <!-- <a class="navbar-brand" href="#">
          <%# image_tag("TTSlogo.png", class: "img-resposive icon") %></a> -->
          <i class="navbar-brand"><%= image_tag("TTSlogo.png", class: "img-resposive icon") %></i>
           
           <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-menubuilder">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar overlay"></span>
            <span class="icon-bar overlay"></span>
            <span class="icon-bar overlay"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse navbar-menubuilder">
            <ul class="nav navbar-nav navbar-left">
              <li class="<%= 'active' if (params[:controller] == 'subjects') and (params[:action] == 'show_list') %>"> <%= link_to "Subjects", show_list_path%>
              </li>  
              <li class="<%= 'active' if (params[:controller] == 'subjects') and (params[:action] == 'show_resources') %>"> <%= link_to "Resources", show_resources_path%>
              </li> 
              <li class="<%= 'active' if params[:controller] == 'application' %>"> <%= link_to "About", about_path%>
              </li> 
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <!-- <div id="container"> -->
                <!-- <div id="user_header"> -->
                  <% if current_user %>
                    Logged in as <%= current_user.email %>.
                    <li><%= link_to "Log Out", logout_path %></li>
                  <% else %>
                    <li><%= link_to "Sign Up", signup_path %></li>
                    <li><%= link_to "Log In",login_path %></li>
                  <% end %>
                <!-- </div> -->

                  <% flash.each do |name, msg| %>
                    <%= content_tag :div, msg, id: "flash_#{name}" %>
                  <% end %>
              <!-- </div> -->
            </ul>
        </div>
    </div>
  </div>
<br><br><br>
      <div class = "container">
        <%= yield %>
      </div>
  </body>
</html>
